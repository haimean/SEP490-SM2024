generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

enum Role {
  USER
  HOST
  ADMIN
}

model Account {
  id                Int                 @id @default(autoincrement())
  email             String              @unique
  password          String
  role              Role                @default(USER)
  otp               String?
  otpExpired        DateTime?
  attributeCourt    AttributeCourt[]
  attributeBranches AttributeBranches[]
  user              User?
  // check mail hợp hệ 
  isVerified        Boolean             @default(false)
  emailToken        String?
  // hoạt động của account
  isActive          Boolean             @default(true)
  createdAt         DateTime            @default(now())
  updatedAt         DateTime            @updatedAt
  TypeCourt         TypeCourt[]
  Branches          Branches[]
  Blog              Blog[]
  Comment           Comment[]
  Booking           Booking[]
  userAvailability  userAvailability[]

  invitationsSend     Invitation[] @relation("seed")
  invitationsReceived Invitation[] @relation("recipient")
  Post                Post[]
  ReviewSend          Review[]     @relation("seed")
  ReviewReceived      Review[]     @relation("recipient")
}

enum Gender {
  MALE
  FEMALE
  OTHER
}

model User {
  id             Int       @id @default(autoincrement())
  account        Account   @relation(fields: [accountId], references: [id])
  accountId      Int       @unique // relation scalar field (used in the `@relation` attribute above)
  fullName       String
  identifierCode String? // cccd
  dob            DateTime?
  numberPhone    String
  gender         Gender
  address        String?
  avatar         String?
  createdAt      DateTime  @default(now())
  updatedAt      DateTime  @updatedAt
}

// trình độ
// Key attribute cở sở sân
model AttributeKeyBranches {
  id                Int                 @id @default(autoincrement())
  name              String              @unique
  description       String?
  isActive          Boolean             @default(true)
  createdAt         DateTime            @default(now())
  updatedAt         DateTime            @updatedAt
  attributeBranches AttributeBranches[]
}

model AttributeBranches {
  id                     Int                   @id @default(autoincrement())
  value                  String
  isPublic               Boolean               @default(false)
  accountId              Int
  attributeKeyBranchesId Int
  isActive               Boolean               @default(true)
  createdAt              DateTime              @default(now())
  updatedAt              DateTime              @updatedAt
  account                Account               @relation(fields: [accountId], references: [id])
  attributeKeyBranches   AttributeKeyBranches? @relation(fields: [attributeKeyBranchesId], references: [id])
  Branches               Branches[]

  @@unique([value, attributeKeyBranchesId])
}

// Key attribute sân
model AttributeKeyCourt {
  id             Int              @id @default(autoincrement())
  name           String           @unique
  description    String?
  isActive       Boolean          @default(true)
  createdAt      DateTime         @default(now())
  updatedAt      DateTime         @updatedAt
  attributeCourt AttributeCourt[]
}

model AttributeCourt {
  id                  Int                @id @default(autoincrement())
  value               String
  isPublic            Boolean            @default(false)
  accountId           Int
  attributeKeyCourtId Int
  createdAt           DateTime           @default(now())
  isActive            Boolean            @default(true)
  updatedAt           DateTime           @updatedAt
  account             Account            @relation(fields: [accountId], references: [id])
  attributeKeyCourt   AttributeKeyCourt? @relation(fields: [attributeKeyCourtId], references: [id])
  court               TypeCourt[]

  @@unique([value, attributeKeyCourtId])
  @@map("category")
}

//   Cở sở có sẽ có nhiều kiểu sân 
model TypeCourt {
  id             Int              @id @default(autoincrement())
  name           String
  attributeCourt AttributeCourt[]
  account        Account          @relation(fields: [accountId], references: [id])
  accountId      Int
  image          String?
  description    String?
  createdAt      DateTime         @default(now())
  updatedAt      DateTime         @updatedAt
  court          Court[]
  priceTypeCourt PriceTypeCourt[]

  @@unique([name, accountId])
}

// Bảng nối cơ sở với sân name của bảng này giống kiểu sân 1 của cơ sở a có kiểu sân b  
model Court {
  id          Int        @id @default(autoincrement())
  name        String
  Branches    Branches?  @relation(fields: [branchesId], references: [id])
  branchesId  Int?
  TypeCourt   TypeCourt? @relation(fields: [typeCourtId], references: [id])
  typeCourtId Int?
  createdAt   DateTime   @default(now())
  updatedAt   DateTime   @updatedAt
  Booking     Booking[]
}

// Bảng cơ sở, có thông tin cơ bản của cơ sở và danh sách sân
model AddressBranch {
  id               Int      @id @default(autoincrement())
  branchesId       Int      @unique
  branches         Branches @relation(fields: [branchesId], references: [id])
  addressLongitude String
  addressLatitude  String
  provinces        String
  districts        String
  wards            String
  detail           String
}

model Branches {
  id                Int                 @id @default(autoincrement())
  name              String
  accountId         Int
  attributeBranches AttributeBranches[]
  court             Court[]
  account           Account             @relation(fields: [accountId], references: [id])
  image             String?
  description       String?
  phone             String
  email             String?
  address           AddressBranch?
  businessLicense   String
  openingHours      DateTime
  closingHours      DateTime
  isAccept          Boolean             @default(false)
  createdAt         DateTime            @default(now())
  updatedAt         DateTime            @updatedAt

  @@unique([name, accountId])
}

enum STATUS_BLOG {
  DRAFT
  PUBLISHED
  ARCHIVED
  BAN
}

model Blog {
  id        Int         @id @default(autoincrement())
  accountId Int
  account   Account     @relation(fields: [accountId], references: [id])
  title     String
  content   String
  image     String
  status    STATUS_BLOG
  createdAt DateTime    @default(now())
  updatedAt DateTime    @updatedAt
  Comment   Comment[]
}

model Comment {
  id              Int         @id @default(autoincrement())
  blogId          Int
  blog            Blog        @relation(fields: [blogId], references: [id])
  accountId       Int
  account         Account     @relation(fields: [accountId], references: [id])
  content         String
  parentCommentId Int? // Sử dụng Int? để cho phép giá trị null
  parentComment   Comment?    @relation("CommentToParentComment", fields: [parentCommentId], references: [id]) // Đặt tên mối quan hệ
  childComments   Comment[]   @relation("CommentToParentComment") // Đặt tên mối quan hệ để khớp với phần trên
  image           String
  status          STATUS_BLOG
  createdAt       DateTime    @default(now())
  updatedAt       DateTime    @updatedAt
}

// Ca đặt
model PriceTypeCourt {
  id          Int        @id @default(autoincrement())
  startTime   DateTime
  endTime     DateTime
  createdAt   DateTime   @default(now())
  updatedAt   DateTime   @updatedAt
  price       Int
  TypeCourt   TypeCourt? @relation(fields: [typeCourtId], references: [id])
  typeCourtId Int?
}

model Booking {
  id          Int          @id @default(autoincrement())
  createdAt   DateTime     @default(now())
  updatedAt   DateTime     @updatedAt
  accountId   Int
  account     Account      @relation(fields: [accountId], references: [id])
  Court       Court?       @relation(fields: [courtId], references: [id])
  courtId     Int?
  dateTime    DateTime
  startTime   DateTime
  endTime     DateTime
  price       Int
  bookingInfo BookingInfo?
  Post        Post?
}

model BookingInfo {
  id          Int      @id @default(autoincrement())
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  name        String
  numberPhone String
  Booking     Booking? @relation(fields: [bookingId], references: [id])
  bookingId   Int?     @unique
}

enum StatusUserAvailability {
  AVAILABLE
  UNAVAILABLE
}

// người rảnh
model userAvailability {
  id        Int                    @id @default(autoincrement())
  createdAt DateTime               @default(now())
  updatedAt DateTime               @updatedAt
  accountId Int
  account   Account                @relation(fields: [accountId], references: [id])
  dateTime  DateTime
  startTime DateTime
  endTime   DateTime
  status    StatusUserAvailability
  provinces String
  districts String
}

enum StatusInvitation {
  AVAILABLE
  UNAVAILABLE
}

// lời mời
model Invitation {
  id                 Int              @id @default(autoincrement())
  createdAt          DateTime         @default(now())
  updatedAt          DateTime         @updatedAt
  accountSendId      Int
  accountSend        Account          @relation("seed", fields: [accountSendId], references: [id])
  accountRecipientId Int
  accountRecipient   Account          @relation("recipient", fields: [accountRecipientId], references: [id])
  postId             Int
  Post               Post             @relation(fields: [postId], references: [id])
  status             StatusInvitation
}

enum Level {
  Y
  TB
  K
  T
  CN
}

enum GenderPost {
  MALE
  FEMALE
  ORTHER
}

// người đặt sân đi tim người
model Post {
  id           Int          @id @default(autoincrement())
  createdAt    DateTime     @default(now())
  updatedAt    DateTime     @updatedAt
  bookingId    Int          @unique
  Booking      Booking      @relation(fields: [bookingId], references: [id])
  desciption   String
  numberMember Int
  MemberPost   MemberPost[]
  Account      Account?     @relation(fields: [accountId], references: [id])
  accountId    Int?
  Invitation   Invitation[]
}

model MemberPost {
  id         Int        @id @default(autoincrement())
  createdAt  DateTime   @default(now())
  updatedAt  DateTime   @updatedAt
  postId     Int
  Post       Post       @relation(fields: [postId], references: [id])
  level      Level
  genderPost GenderPost
  price      String     @default("Chia đều")
}

model Review {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  accountSendId      Int
  accountSend        Account @relation("seed", fields: [accountSendId], references: [id])
  accountRecipientId Int
  accountRecipient   Account @relation("recipient", fields: [accountRecipientId], references: [id])
  rating             Int
  comment            String
}

// TABLE notification {
//   notification_id int [pk, increment]
//   user_id int [ref: > user.user_id]
//   message text
//   type ENUM('BLOG', 'COMMENT', 'INVITATION', 'BOOKING')
//   status ENUM('UNREAD', 'READ')
//   blog_id int [ref: > blog.blog_id]
//   comment_id int [ref: > comment.comment_id]
//   invitation_id int [ref: > invitation.invitation_id]
//   created_at timestamp
//   updated_at timestamp
// }

// //Tố cáo
// TABLE report as report {
//   report_id int [pk, increment]
//   reported_by int [ref: > user.user_id]
//   reported_user_id int [ref: > user.user_id]
//   description json
//   status ENUM('PENDING', 'REVIEWED', 'RESOLVED')
//   created_at timestamp
//   updated_at timestamp
// }

// //Thống kê
// TABLE statistics as statistics {
//   statistics_id int [pk, increment]
//   branch_id int [ref: > branch.branch_id]
//   metric_name varchar(100)
//   metric_value varchar(100)
//   created_at timestamp
//   updated_at timestamp
// }
